You are an expert annotator for Vietnamese social media sarcasm detection.

Your ONLY job in this step: analyze the TEXT to determine if it is sarcastic **on its own**, without any image.

---

## INPUT

[TEXT]
{text}

[OCR_TEXT]
{ocr_text}

---

## INSTRUCTIONS

Imagine you received this as a plain text message, with NO image attached.
Ask yourself: "Would an ordinary Vietnamese person reading only this text recognize it as sarcasm?"

### Step-by-step reasoning (CoT)

**Step 1 — Identify surface signals**
Scan the text for ANY of these markers:

| # | Signal | Vietnamese example |
|---|--------|--------------------|
| ① | Internal contradiction: opposing words/logic in the same sentence | "Đi muộn được sếp khen, vui quá" |
| ② | Hyperbole applied to something bad/trivial | "Đỉnh nóc kịch trần", "Siêu phẩm", "Tuyệt tác nhân loại" |
| ③ | Praise for an obviously bad situation | "Ngập đường thật tiện lợi", "Tắc 3 tiếng, trải nghiệm tuyệt vời" |
| ④ | Emoji reversal: positive text + negative emoji, or vice versa | "Giỏi thật 🤡", "Thành công rồi 💀" |
| ⑤ | Verbal irony: using good words to mean something bad | "Thật là một ngày tuyệt vời" after listing disasters |

**Step 2 — Apply Vietnamese internet register**
Vietnamese social media sarcasm is often conveyed through:
- Understated tone ("ừ đúng rồi", "hay thật", "giỏi ghê") — these are frequently sarcastic even without strong markers
- Repetition for mockery ("giỏi giỏi", "hay hay")
- Ellipsis/trailing off ("thôi rồi...", "hết nước chấm luôn")
- Backhanded compliments that only make sense sarcastically in context

**Step 3 — Make the call**

Assign **T=1** if at least ONE signal from Step 1 is present, OR if the Step 2 register strongly implies sarcasm.

Assign **T=0** if:
- The text is a genuine question, greeting, or neutral statement
- Emotion is authentic (genuinely sad, happy, or angry) with no reversal cues
- The text is too vague to call (e.g., "Alo", "OK", "Ừ" alone → T=0)

⚠️ CALIBRATION BIAS: When in doubt between T=0 and T=1, **lean toward T=1**.
Vietnamese sarcasm rarely announces itself. A `needs_human_check=1` flag exists precisely for borderline cases.

---

## OUTPUT

Return ONLY this JSON, nothing else:

```json
{
  "T": 0,
  "T_confidence": "high | medium | low",
  "T_signals": ["list the specific signals found, e.g., '① contradiction: khen + tình huống xấu'"],
  "T_reason": "One sentence explaining the decision in Vietnamese",
  "has_emoji": 0,
  "needs_human_check": 0
}
```

**has_emoji**: 1 if [TEXT] contains at least one emoji character, else 0.
**needs_human_check**: 1 if T_confidence is "low" OR the text is ambiguous slang/dialect you are unsure about.
